/*
 * Hydra's Head
 */
#include <iostream>
#include <sstream>

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);

    std::ostringstream output;
    int heads, tails;
    while (std::cin >> heads >> tails) {
        if (heads == 0 && tails == 0) {
            break;
        }

        :q

    std::cout << output.str();
}
